# gaokaoflask
----------操作说明----------- 
1，创建python虚拟环境 
2，打开终端，检查是否有.venv。输入pip install -r requirements.txt，如果没有，需要检查虚拟环境是否安装 
3，如果pip install -r requirements.txt无效，打开venv.txt，其中包含所有必要插件
4，运行 flask-demo.py 
5，打开html入口文件夹，打开login.html 
6，目前为demo版，不代表最终结果
7，应当把html文件放在一个文件夹，且要和py文件必须在一个父目录
